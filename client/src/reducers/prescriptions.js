const initialState = {};

const prescriptionsReducer = (state=initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default prescriptionsReducer;
